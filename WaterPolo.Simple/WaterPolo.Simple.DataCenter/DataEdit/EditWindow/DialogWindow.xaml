<Window x:Class="WaterPolo.Simple.DataCenter.DataEdit.EditWindow.DialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WaterPolo.Simple.DataCenter.DataEdit.EditWindow"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="300" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Row="0">
            <Label Content="{Binding Title}"></Label>
        </Grid>
        <ContentControl Grid.Row="1" Content="{Binding Content}"></ContentControl>
        <Grid Row="2">
            <StackPanel Margin="5" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Width="80" Margin="5" Click="Ok_Click">OK</Button>
                <Button Width="80" Margin="5" Click="Cancel_Click">Cancel</Button>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
